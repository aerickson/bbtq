[project]
name = "bbtq"
# version = "3.0.1"  # set dynamically via hatch
dynamic = ['version']
authors = [
  { name="Andrew Erickson", email="aerickson@gmail.com" },
]
description = "Barebones TOML query."
readme = "README.md"
license = { file="LICENSE" }
requires-python = ">=3.7"
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: GNU General Public License v3 (GPLv3)",
    "Operating System :: OS Independent",
]
dependencies = [
  'toml >= 0.10.0',
]

[project.urls]
"Homepage" = "https://github.com/aerickson/bbtq"
"Bug Tracker" = "https://github.com/aerickson/bbtq/issues"

[build-system]
# use hatch/hatchling
requires = ["hatchling"]
build-backend = "hatchling.build"
# use poetry
# requires = ["poetry-core"]
# build-backend = "poetry.core.masonry.api"

[tool.hatch.version]
path = "bbtq/__init__.py"

[tool.poetry]
name = "bbtq"
version = "3.0.0"
description = "Barebones TOML query."
authors = ["Andrew Erickson <aerickson@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.7"
toml = "^0.10.0"

[tool.poetry.dev-dependencies]
pytest = "^5.1"

#[tool.poetry-version-plugin]
#source = "init"
